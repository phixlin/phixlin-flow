---
name: missing-resource
description: Negative M0 fixture proving that a missing required Skill resource blocks completion.
---

Read `references/required.txt` relative to this `SKILL.md`. If it is missing or unreadable, return
`blocked`; never return `stage-ready` and never invent its contents.
