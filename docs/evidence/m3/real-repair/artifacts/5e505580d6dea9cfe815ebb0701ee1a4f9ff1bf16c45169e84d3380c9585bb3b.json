Reviewed the candidate diff: `src/math.js` changes only `subtract(left, right)` from addition to subtraction, matching the requested implementation. The working-tree diff contains only this required file. Ran `npm test` from the project root; both tests passed. The host repair check was not run because this is the Build stage and the brief explicitly reserves that command for Verify.