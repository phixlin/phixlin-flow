# AGENTS.md

phixlin-flow 是面向编码 Agent 的、可适配多平台的确定性交付 Harness。它将需求澄清、规格制定、代码实现、机器验证、人工确认和知识归档组织为可审计、可恢复的工作流。改 src/ 前先读 README。

## 命令

| 命令 | 用途 |
|---|---|
| pnpm test | 单元测试 |
| pnpm typecheck | 类型检查 |
| pnpm lint | 静态检查 |
| pnpm test:smoke | 真实入口冒烟测试 |
| pnpm check:all | 全量本地门禁(CI 跑的就是它) |

改了什么就跑什么;不确定就跑 typecheck,CI 会兜底。
绝不为了提交或推送重复跑已通过的检查。

## 规范

- 错误大声失败:配置错误在加载时抛出,不静默跳过、不吞错继续。
- 空 catch 必须注释写明吞掉了什么、为什么别的异常到不了这里;try 块限一条语句。
- 有静态类型保证的同进程值不加运行时校验;校验只放在外部边界(用户输入、文件、网络、子进程)。
- 每个抽象、配置项、防御分支必须有一个当前使用者;没有就删,不留给"将来"。
- 注释写契约(前置/后置/为什么),不复述代码,不使用比喻。
- 测试描述行为。改过时的行为要连测试一起改,并在 commit message 里解释为什么行为变了。
- 禁止通过改断言、删测试、加 ignore 注释让测试变绿;失败先怀疑实现。
- e2e 断言验证外部世界(重读文件、重跑命令),不探测程序自己打印的关键词。
- 持久化用户数据的格式只加新版本不改旧的;迁移路径先写测试再写实现。
- TODO 三级:FIXME 阻塞下个 release / TODO 尽快 / XXX 有朝一日。
- 非平凡改动(新模块、行为变更、格式迁移)在同一次 commit 附 docs/decisions/ 记录。
- 文档随代码同 commit 更新;文件结尾恰好一个换行符。

## 失败时

- 测试失败 → 定位根因修复;根因在测试本身才动测试,并在 commit 里说明。
- 需要在多个方案间做选择 → 停下问我,列选项和你的推荐,不要自己选一个然后执行。
- 不确定要不要加(抽象/配置/防御代码/依赖)→ 默认不加。

## 决策记录

非平凡改动附 `docs/decisions/NNNN-slug.md`(NNNN 递增)。三段式:
Context(当时的约束)、Decision(选了什么)、Consequences(代价与不做的部分)。
已归档的记录冻结不改;新决策新开文件推翻旧决策。
